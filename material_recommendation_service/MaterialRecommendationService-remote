# -*- coding:utf-8 -*-
#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (1.0.0-dev)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import TSSLSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from material_recommendation_service import MaterialRecommendationService
from material_recommendation_service.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print('')
  print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] function [arg1 [arg2...]]')
  print('')
  print('Functions:')
  print('  string article_recommend(string input_arg)')
  print('  string recommend_topn_topic(string input_arg)')
  print('  string recommend_tags(string input_arg)')
  print('  string recommend_list(string input_arg)')
  print('  string recommend_plan(string input_arg)')
  print('  string recommend_news(string input_arg)')
  print('')
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
  ssl = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSSLSocket.TSSLSocket(host, port, validate=False) if ssl else TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = MaterialRecommendationService.Client(protocol)
transport.open()

if cmd == 'article_recommend':
  if len(args) != 1:
    print('article_recommend requires 1 args')
    sys.exit(1)
  pp.pprint(client.article_recommend(args[0],))

elif cmd == 'recommend_topn_topic':
  if len(args) != 1:
    print('recommend_topn_topic requires 1 args')
    sys.exit(1)
  pp.pprint(client.recommend_topn_topic(args[0],))

elif cmd == 'recommend_tags':
  if len(args) != 1:
    print('recommend_tags requires 1 args')
    sys.exit(1)
  pp.pprint(client.recommend_tags(args[0],))

elif cmd == 'recommend_list':
  if len(args) != 1:
    print('recommend_list requires 1 args')
    sys.exit(1)
  pp.pprint(client.recommend_list(args[0],))

elif cmd == 'recommend_plan':
  if len(args) != 1:
    print('recommend_plan requires 1 args')
    sys.exit(1)
  pp.pprint(client.recommend_plan(args[0],))

elif cmd == 'recommend_news':
  if len(args) != 1:
    print('recommend_news requires 1 args')
    sys.exit(1)
  pp.pprint(client.recommend_news(args[0],))

else:
  print('Unrecognized method %s' % cmd)
  sys.exit(1)

transport.close()
